export const WINDOW_MATH_MEDIA = {
  PREFERS_COLOR_SCHEME: "(prefers-color-scheme: dark)",
} as const;

export const INDEX_DB_NAME = {
  FLASH_CARDS: "flashCards",
} as const;

export const INDEX_DB_STORE = {
  CARDS: "cards",
} as const;
